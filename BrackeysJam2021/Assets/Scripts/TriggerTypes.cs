using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Use this enum to determine which type of prompt you want to use
/// </summary>
public enum TriggerTypes
{
    NONE = 0,
    IMAGE = 1,
    TEXT = 2,
    TEXT_IMAGE = 3,
    ANIM = 4
}
